<?xml version="1.0" encoding="utf-8" ?>
<local:BaseContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MyPassword.Pages"
             x:Class="MyPassword.Pages.PasswordListPage">
    <ContentPage.Content>
        <ListView 
            ItemsSource="{Binding PasswordList}" 
            RowHeight="84"
            ItemTapped="ListView_ItemTapped"
            SeparatorVisibility="Default"
            SeparatorColor="LightGray">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Grid VerticalOptions="Center" Margin="12,3">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Image Source="{Binding Icon}" WidthRequest="30" Margin="0,0,12,0" Grid.Row="0" Grid.RowSpan="2" Grid.Column="0" VerticalOptions="Start"/>
                            <Label Text="{Binding Name}" FontSize="Small" TextColor="Gray" Grid.Row="0" Grid.Column="1"/>
                            <Label Text="{Binding Account}" FontSize="Small" TextColor="DarkGray" Grid.Row="1" Grid.Column="1"/>
                            <Label Text="{Binding Description}" FontSize="Small" TextColor="DarkGray" Grid.Row="2" Grid.Column="1"/>
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</local:BaseContentPage>