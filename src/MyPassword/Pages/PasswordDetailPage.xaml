<?xml version="1.0" encoding="utf-8" ?>
<local:BaseContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MyPassword.Pages"
             xmlns:controls="clr-namespace:MyPassword.Controls"
             x:Class="MyPassword.Pages.PasswordDetailPage">
    <ContentPage.Content>
        <StackLayout Margin="24">
            <Entry Placeholder="添加标题" Text="{Binding Title}" FontSize="Medium"/>
            <Label Text="账号" HorizontalOptions="Start" FontSize="Medium"/>
            <Entry Placeholder="请输入账号" Text="{Binding Account}" FontSize="Medium"/>
            <Label Text="密码" HorizontalOptions="Start" FontSize="Medium"/>
            <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand">
                <Entry Placeholder="您的密码" FontSize="Medium" IsPassword="False" Text="{Binding Password}" HorizontalOptions="FillAndExpand"/>
                <Button Text="生成" WidthRequest="60" HeightRequest="24" Margin="0" CornerRadius="5" BackgroundColor="#2196F3" TextColor="White" FontSize="14" Command="{Binding GenerateCommand}"/>
            </StackLayout>
            <controls:PwdConditionView BindingContext="{Binding ConditionViewModel}"/>
            <Entry Placeholder="备注信息" Text="{Binding Description}" FontSize="Medium"/>
        </StackLayout>
    </ContentPage.Content>
</local:BaseContentPage>