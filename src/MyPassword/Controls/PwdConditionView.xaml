<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:MyPassword.Controls"
             x:Class="MyPassword.Controls.PwdConditionView">
    <ContentView.Content>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <StackLayout Grid.Column="0" Grid.Row="0" Orientation="Horizontal">
                <controls:Checkbox Checked="{Binding CheckedNumber}"/>
                <Label Text="数字" VerticalOptions="Center"/>
            </StackLayout>
            <StackLayout Grid.Column="1" Grid.Row="0" Orientation="Horizontal">
                <controls:Checkbox Checked="{Binding CheckedLowerCase}"/>
                <Label Text="小写字母" VerticalOptions="Center"/>
            </StackLayout>
            <StackLayout Grid.Column="0" Grid.Row="1" Orientation="Horizontal">
                <controls:Checkbox Checked="{Binding CheckedUpperCase}"/>
                <Label Text="大写字母" VerticalOptions="Center"/>
            </StackLayout>
            <StackLayout Grid.Column="1" Grid.Row="1" Orientation="Horizontal">
                <controls:Checkbox Checked="{Binding CheckedCharactor}"/>
                <Label Text="特殊字符" VerticalOptions="Center"/>
            </StackLayout>
            <StackLayout Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="2" Orientation="Horizontal">
                <Label Text="密码长度：" VerticalOptions="Center"/>
                <Label Text="{Binding Length}" VerticalOptions="Center"/>
            </StackLayout>
            <Slider Grid.Column="0" Maximum="20" Minimum="6" Value="{Binding Length,Mode=TwoWay}" Grid.ColumnSpan="2" Margin="0,12" Grid.Row="3"/>
        </Grid>
    </ContentView.Content>
</ContentView>